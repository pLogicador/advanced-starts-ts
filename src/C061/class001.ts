// Importing Javascript in TypeScript

/*
import { sum } from './module';

const result = sum(5,5);
console.log(result);

*/
// In tsconfig uncomment the ("alowJs": true ) to allow importing the js file
